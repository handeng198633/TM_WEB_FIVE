<%= render 'refinery/admin/search_header', :url => refinery.fit_orders_admin_fit_orders_path %>
<div class='pagination_container'>
  <% if @fit_orders.any? %>
    <%= render 'fit_orders' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
