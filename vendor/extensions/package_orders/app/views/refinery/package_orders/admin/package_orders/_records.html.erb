<%= render 'refinery/admin/search_header', :url => refinery.package_orders_admin_package_orders_path %>
<div class='pagination_container'>
  <% if @package_orders.any? %>
    <%= render 'package_orders' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
